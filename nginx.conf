user nginx;
events {
    worker_connections 1000;
}
http {
    server {
        listen 4000;
        resolver 127.0.0.11;
        location / {
            proxy_pass http://rest-api:8888;
        }
    }
}